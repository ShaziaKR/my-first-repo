# HTML generation examples

Run the provided Python script to generate a simple HTML file.

Basic run:

```bash
python generate_html.py
```

Custom filename, title and body:

```bash
python generate_html.py -o mypage.html --title "My Page" --body "<h1>Hi</h1><p>Generated automatically.</p>"
```

The script writes the output file in the current directory.
